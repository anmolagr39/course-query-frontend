services:
  - type: web
    name: course-query-app
    runtime: python
    buildCommand: npm install --legacy-peer-deps && npm run build && pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.8 